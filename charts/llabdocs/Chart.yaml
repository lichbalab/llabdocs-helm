apiVersion: v2
name: llabdocs
description: A Helm chart for LLabDocs service
type: application
version: 1.9-helm
appVersion: "2024.1"

# Примечание: ingress-nginx controller вынесен из чарта — ставится один раз на кластер
# (helm upgrade --install ingress-nginx ingress-nginx --repo https://kubernetes.github.io/ingress-nginx ...).
# Чарт только декларирует нативные Ingress-ресурсы с ingressClassName: nginx.
